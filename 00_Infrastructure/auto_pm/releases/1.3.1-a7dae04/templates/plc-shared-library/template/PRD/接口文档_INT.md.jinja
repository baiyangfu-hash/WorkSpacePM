# 接口文档（INT）- {{ library_name }}

> 库名称: {{ library_name }}
> 库描述: {{ description }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}

## 1. 接口概述

### 1.1 文档目的
本文档定义 {{ library_name }} 公共库中所有函数（FC）、功能块（FB）、结构体（ST）的接口规范，包括参数定义、数据类型、调用约定与版本兼容性。所有调用方必须严格遵循本文档的接口契约，禁止假设未文档化的参数行为。

### 1.2 接口分类
{{ library_name }} 的接口按模块组织，对应以下目录：

| 模块目录 | 接口类型 | 命名前缀 | 说明 |
|---------|---------|---------|------|
| actuator/ | FB | FB_ | 执行器功能块 |
| communication/ | FB | FB_ | 通信功能块 |
| convert/ | FC | FC_ | 数据转换函数 |
| counter/ | FB | FB_ | 计数器功能块 |
| edge/ | FB | FB_ | 边沿检测功能块 |
| log/ | FC | FC_ | 日志函数 |
| pulse/ | FB | FB_ | 脉冲生成功能块 |
| timer/ | FB | FB_ | 定时器功能块 |
| types/ | ST | ST_ | 类型定义 |

## 2. 命名规范

### 2.1 功能块命名（FB）
- 格式：`FB_<编号>_<功能名>` 或 `FB_<功能名>`
- 示例：`FB_1011_CylinderControl`、`FB_TON`、`FB_R_TRIG`
- 编号规则：4位数字，前两位为模块类别（10=actuator, 20=communication...）

### 2.2 函数命名（FC）
- 格式：`FC_<源类型>_TO_<目标类型>` 或 `FC_<功能名>`
- 示例：`FC_DINT_TO_TIME`、`FC_TIME_TO_INT`、`FC_LogMsg`
- 转换函数必须明确标注源类型与目标类型

### 2.3 结构体命名（ST）
- 格式：`ST_<业务名>`
- 示例：`ST_Cylinder`、`ST_HandshakeBits`、`ST_ServoAxis`
- 业务名使用大驼峰风格，语义清晰

### 2.4 参数命名
| 参数类别 | 前缀 | 示例 |
|---------|------|------|
| 输入参数（VAR_INPUT） | i_ | i_bStart, i_rSpeed |
| 输出参数（VAR_OUTPUT） | q_ | q_bRunning, q_rPosition |
| 输入输出参数（VAR_IN_OUT） | io_ | io_stData |
| 静态变量（VAR） | s_ | s_tonDelay |
| 临时变量（VAR_TEMP） | t_ | t_nCount |

类型前缀：`b`=BOOL, `n`=INT, `d`=DINT, `r`=REAL, `s`=STRING, `st`=STRUCT, `a`=ARRAY

## 3. 函数/功能块接口规范

### 3.1 参数声明格式
所有 FB/FC 的参数必须按以下顺序声明：
1. VAR_INPUT：输入参数
2. VAR_OUTPUT：输出参数
3. VAR_IN_OUT：输入输出参数
4. VAR：静态变量（仅 FB）
5. VAR_TEMP：临时变量

### 3.2 定时器接口规范（timer/）
定时器功能块遵循 LSP-903 规范：
- PT（预设时间）参数类型为 DINT，单位毫秒
- ET（经过时间）参数类型为 DINT，单位毫秒
- 禁止使用 TIME 类型（LSP 编译器不支持）
- 调用必须包含完整参数：IN/PT/Q/ET，Q 参数不能为空

示例接口（FB_TON）：
```
FB_TON
  VAR_INPUT
    IN: BOOL;      // 启动信号
    PT: DINT;      // 预设时间(ms)
  END_VAR
  VAR_OUTPUT
    Q: BOOL;       // 定时到达
    ET: DINT;      // 经过时间(ms)
  END_VAR
```

### 3.3 计数器接口规范（counter/）
计数器功能块提供递增、递减、双向计数：
- CU：递增计数输入（BOOL，上升沿触发）
- CD：递减计数输入（BOOL，上升沿触发）
- R：复位输入（BOOL）
- PV：预设值（INT/DINT）
- Q：计数到达输出（BOOL）
- CV：当前计数值（INT/DINT）

### 3.4 边沿检测接口规范（edge/）
- CLK：触发信号输入（BOOL）
- Q：边沿检测输出（BOOL，持续一个扫描周期）

### 3.5 转换函数接口规范（convert/）
转换函数为纯函数（无副作用），接口格式：
- 输入：源类型值
- 输出：目标类型值
- 示例：`FC_DINT_TO_TIME(dValue: DINT) : TIME`

## 4. 调用示例

### 4.1 定时器调用示例
```
// 接通延时定时器
#tonDelay(
  IN := #i_bStart,
  PT := 1000,       // 1000ms = 1s
  Q => #q_bDelayDone,
  ET => #dElapsed
);
```

### 4.2 边沿检测调用示例
```
// 上升沿检测
#rTrigStart(CLK := #i_bStart, Q => #q_bStartEdge);
```

### 4.3 转换函数调用示例
```
// DINT 转 TIME
#tDelay := FC_DINT_TO_TIME(#dDelayMs);
```

### 4.4 计数器调用示例
```
// 递增计数器
#ctuParts(
  CU := #q_bPartReady,
  R := #i_bReset,
  PV := 100,
  Q => #q_bCountDone,
  CV => #nPartCount
);
```

## 5. 版本兼容性说明

### 5.1 版本号规则
- 主版本.次版本.修订号（如 V1.0.0）
- 主版本变更：接口破坏性变更（参数增删改）
- 次版本变更：新增功能块/函数，向后兼容
- 修订号变更：Bug 修复，接口不变

### 5.2 兼容性承诺
- 同一主版本内保证向后兼容
- 接口变更必须升级主版本号
- 废弃接口需保留至少一个主版本周期，并标注 @deprecated

### 5.3 依赖管理
- 公共库本身不引用其他库（.plc.json 中 libraries 为空数组）
- 调用方通过 .plc.json 的 libraries 字段引用本库
- 路径使用相对路径，分隔符为正斜杠 `/`

## 6. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本 | auto-pm |
