"""pytest 共享 fixtures"""

from __future__ import annotations

import os
from pathlib import Path

import pytest


@pytest.fixture
def tmp_workspace(tmp_path: Path) -> str:
    """临时工作空间目录，测试结束后自动清理"""
    return str(tmp_path)


@pytest.fixture
def sample_file(tmp_workspace: str) -> str:
    """创建一个示例文件并返回路径"""
    file_path = os.path.join(tmp_workspace, "sample.txt")
    with open(file_path, "w", encoding="utf-8") as f:
        f.write("sample content")
    return file_path
