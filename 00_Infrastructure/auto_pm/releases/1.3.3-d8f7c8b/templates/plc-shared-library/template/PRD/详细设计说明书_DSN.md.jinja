# 详细设计说明书（DSN）- {{ library_name }}

> 库名称: {{ library_name }}
> 库描述: {{ description }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}

## 1. 设计概述

### 1.1 设计目标
{{ library_name }} 旨在提供一套标准化、可复用、跨平台兼容的 PLC 公共函数库。本设计文档描述库的架构划分、数据结构设计、函数/功能块设计规范与错误处理策略，作为开发与维护的依据。

### 1.2 设计原则
- **模块化设计**：按功能类别划分目录，模块间低耦合
- **单一职责**：每个 FB/FC 只负责一项明确功能
- **无状态优先**：FC 函数为纯函数，FB 状态通过实例化隔离
- **规范遵循**：严格遵循 LSP-905/904/903/906 规范
- **可测试性**：关键逻辑可独立测试，不依赖外部硬件

## 2. 架构设计

### 2.1 模块划分
{{ library_name }} 采用按功能类别划分的扁平目录结构，每个目录对应一类功能：

| 模块目录 | 职责 | 依赖关系 | 设计要点 |
|---------|------|---------|---------|
| actuator/ | 执行器控制 | 依赖 types/、timer/ | 封装气缸、电机等执行元件的状态机 |
| communication/ | 设备通信 | 依赖 types/、edge/ | 实现上下游设备握手协议 |
| convert/ | 数据转换 | 无依赖 | 纯函数，类型间转换 |
| counter/ | 计数器 | 无依赖 | 递增/递减/双向计数 |
| edge/ | 边沿检测 | 无依赖 | 上升沿/下降沿检测 |
| log/ | 日志输出 | 无依赖 | 运行日志格式化 |
| pulse/ | 脉冲生成 | 依赖 timer/ | 周期脉冲、节拍发生 |
| timer/ | 定时器 | 无依赖 | TON/TOF/TP/TONR |
| types/ | 类型定义 | 无依赖 | 业务结构体定义 |

### 2.2 依赖方向
- `types/` 是基础模块，被其他模块依赖
- `timer/` 被 `actuator/`、`pulse/` 依赖
- `edge/` 被 `communication/` 依赖
- `convert/`、`counter/`、`log/` 为独立模块，无内部依赖
- 禁止循环依赖

### 2.3 目录结构
```
{{ library_name }}/
├── actuator/        # 执行器 FB
├── communication/   # 通信 FB
├── convert/         # 转换 FC
├── counter/         # 计数器 FB
├── edge/            # 边沿检测 FB
├── log/             # 日志 FC
├── pulse/           # 脉冲生成 FB
├── timer/           # 定时器 FB
├── types/           # 类型定义 ST
├── PRD/             # 项目文档
├── .plc.json        # 库配置
└── README.md        # 库说明
```

## 3. 数据结构设计

### 3.1 类型定义规范（types/）
所有业务结构体统一定义在 types/ 目录，遵循以下规范：
- 命名：`ST_<业务名>`（大驼峰）
- 字段命名：小驼峰 + 类型前缀（如 `bReady`、`nCount`、`rSpeed`）
- 每个结构体配套独立 .scl 文件
- 字段必须有注释说明语义与单位

### 3.2 典型结构体示例

#### ST_Cylinder（气缸状态）
```
TYPE ST_Cylinder :
STRUCT
  bExtendCmd: BOOL;      // 伸出命令
  bRetractCmd: BOOL;     // 缩回命令
  bExtended: BOOL;       // 伸出到位信号
  bRetracted: BOOL;      // 缩回到位信号
  bError: BOOL;          // 故障标志
  nErrorCode: INT;       // 故障码
END_STRUCT
END_TYPE
```

#### ST_HandshakeBits（握手信号）
```
TYPE ST_HandshakeBits :
STRUCT
  bRequest: BOOL;        // 请求信号
  bAcknowledge: BOOL;    // 应答信号
  bBusy: BOOL;           // 忙碌标志
  bDone: BOOL;           // 完成标志
END_STRUCT
END_TYPE
```

## 4. 函数设计（FC）

### 4.1 FC 设计规范
- FC 为纯函数，无静态状态，无副作用
- 相同输入必须产生相同输出
- 参数全部通过 VAR_INPUT 传入，结果通过返回值或 VAR_OUTPUT 输出
- 禁止在 FC 内部访问全局变量

### 4.2 转换函数设计（convert/）
转换函数实现类型间的无损或有损转换：
- `FC_DINT_TO_TIME`：DINT 毫秒值转 TIME
- `FC_TIME_TO_DINT`：TIME 转 DINT 毫秒值
- `FC_INT_TO_TIME`：INT 秒值转 TIME
- `FC_TIME_TO_INT`：TIME 转 INT 秒值

设计要点：
- 转换必须明确单位（毫秒/秒）
- 溢出情况返回最大/最小值并记录日志
- 禁止隐式类型转换

### 4.3 日志函数设计（log/）
- `FC_LogMsg`：格式化日志消息输出
- 日志级别：INFO/WARN/ERROR
- 日志内容包含时间戳、级别、消息体

## 5. 功能块设计（FB）

### 5.1 FB 设计规范
- FB 通过实例化隔离状态，每个调用点使用独立实例
- 状态机使用 ENUM 或 INT 状态字
- VAR_TEMP 必须在 VAR 块中定义，禁止在程序中间定义
- 调用必须包含完整参数，禁止省略

### 5.2 定时器功能块设计（timer/）
定时器 FB 遵循 LSP-903 规范：
- **FB_TON**（接通延时）：IN=TRUE 开始计时，到达 PT 后 Q=TRUE
- **FB_TOF**（断开延时）：IN=FALSE 开始计时，到达 PT 后 Q=FALSE
- **FB_TP**（脉冲）：IN 上升沿触发，输出固定宽度脉冲
- **FB_TONR**（累加定时器）：可累计多次计时，需手动复位

设计要点：
- PT/ET 使用 DINT 类型（毫秒），禁止 TIME
- 定时器数组必须使用独立实例，禁止数组索引调用（LSP-906）
- Q 参数不能为空

### 5.3 计数器功能块设计（counter/）
- **FB_CTU**（递增计数）：CU 上升沿递增，到达 PV 时 Q=TRUE
- **FB_CTD**（递减计数）：CD 上升沿递减，到达 0 时 Q=TRUE
- **FB_CTUD**（双向计数）：支持递增递减，独立复位与加载

### 5.4 边沿检测功能块设计（edge/）
- **FB_R_TRIG**（上升沿）：CLK 上升沿时 Q 输出一个扫描周期脉冲
- **FB_F_TRIG**（下降沿）：CLK 下降沿时 Q 输出一个扫描周期脉冲
- 内部使用静态变量保存上一周期状态

### 5.5 执行器功能块设计（actuator/）
执行器 FB 封装状态机，典型设计：
- **FB_1011_CylinderControl**：气缸控制，含伸出/缩回/到位检测/超时报警
- **FB_1012_ConveyorMotor**：传送带电机控制，含启停/方向/速度/故障
- 状态机：IDLE → RUNNING → DONE / ERROR

## 6. 错误处理设计

### 6.1 错误处理原则
- 错误状态通过 q_bError 输出，错误码通过 q_nErrorCode 输出
- 错误码采用模块前缀 + 序号（如 1011xx 表示气缸控制错误）
- 错误状态需保持，直到复位信号清除

### 6.2 错误预防（LSP-906）
- 变量前缀必须与类型匹配
- 定时器数组必须使用独立实例
- VAR_TEMP 必须在 VAR 块中定义
- 禁止在 FB 内部使用循环等待

### 6.3 超时处理
- 执行器动作必须配置超时检测（使用 timer/ 模块）
- 超时触发错误状态，停止动作输出
- 超时时间通过参数可配置

## 7. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本 | auto-pm |
