# {{ project_name }} 自动工艺流程图

> **项目编号**: {{ project_id }}  
> **项目名称**: {{ project_name }}  
> **版本号**: {{ version }}  
> **编制人**: {{ author }}  
> **更新日期**: {{ now().strftime('%Y-%m-%d') if now is defined else '2026-08-23' }}  
> **规范依据**: DEV-001 / STD-850 / LSP-907  

---

## 1. 工艺流程总览

{{ project_name }} 自动控制工艺流程分为：**就绪准备** $\rightarrow$ **工位1 进料夹紧** $\rightarrow$ **动作加工与检测** $\rightarrow$ **出料交接** $\rightarrow$ **复位就绪**。

```mermaid
graph TD
    S0([STEP 0: 系统就绪与待机]) -->|自动运行使能 + 安全门关闭| S1[STEP 1: 工件入料检测]
    S1 -->|入料光电信号 ON + 延时消抖| S2[STEP 2: 工位1 进料输送]
    S2 -->|到达加工位| S10[STEP 10: 夹紧气缸伸出 + 顶升到位]
    S10 -->|夹紧 & 顶升磁簧到位| S20[STEP 20: 执行工艺加工动作]
    S20 -->|加工完成 + 外部允许出料| S30[STEP 30: 气缸复位 + 出料输送]
    S30 -->|出料光电离开 + 工位清空| S0
```

---

## 2. 状态转移控制表

| 步骤编号 | 状态定义 | 激活输出 | 转移条件 | 超时判定 | 下一状态 |
| :---: | :--- | :--- | :--- | :---: | :---: |
| **0** | `STEP_IDLE` | 全部输出关闭 | 自动使能 + 气压正常 + 安全门关闭 | 无 | `1` |
| **1** | `STEP_WAIT_MATERIAL` | 等待入料 | 入料传感器触发 (消抖 100ms) | 无 | `2` |
| **2** | `STEP_FEEDING` | 启动进料输送电机 | 到达加工位传感器触发 | 10.0s | `10` |
| **10** | `STEP_CLAMP` | 夹紧与顶升电磁阀 | 气缸伸出磁簧 ON | 3.0s | `20` |
| **20** | `STEP_PROCESS` | 执行主动作机构 | 工艺动作完成信号触发 | 15.0s | `30` |
| **30** | `STEP_RELEASE_UNLOAD` | 气缸缩回 + 出料输送 | 气缸缩回到位 + 工件离开 | 10.0s | `0` |
