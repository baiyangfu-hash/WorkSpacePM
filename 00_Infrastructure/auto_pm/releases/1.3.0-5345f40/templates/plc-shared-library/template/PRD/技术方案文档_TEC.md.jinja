# 技术方案文档（TEC）- {{ library_name }}

> 库名称: {{ library_name }}
> 库描述: {{ description }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}

## 1. 技术方案概述

### 1.1 项目背景
{{ library_name }} 是为解决 PLC 自动化项目中功能块重复开发、实现风格不一致、维护成本高的问题而设计的公共函数库。本库沉淀通用、经验证的功能块，通过标准化接口与版本化管理，实现跨项目复用。

### 1.2 技术选型

| 类别 | 选型 | 说明 |
|------|------|------|
| 编程语言 | SCL/ST | 遵循 IEC 61131-3 标准，跨平台可移植 |
| 主目标平台 | 西门子 S7-1200/1500 | TIA Portal SCL 编译器 |
| 兼容平台 | 三菱 GX3 | ST 语言兼容 |
| 编程规范 | LSP-905 | SCL 编程主规范 |
| 注释规范 | LSP-904 | 注释格式规范 |
| 定时器规范 | LSP-903 | FB_TON 使用规则 |
| 错误预防 | LSP-906 | 实战 bug 检查清单 |
| 项目配置 | LSP-907 | .plc.json 配置规范 |
| 版本管理 | Git | 配合 Git hooks 强制规范检查 |

### 1.3 设计目标
- **复用性**：一次开发，多项目复用
- **标准化**：统一命名、接口、实现风格
- **可靠性**：经过单元测试与项目验证
- **可维护性**：配套完整 PRD 文档，版本化管理
- **跨平台**：兼容西门子与三菱主流 PLC

## 2. 实现方案

### 2.1 各模块技术实现要点

#### 2.1.1 timer/（定时器模块）
- 实现 FB_TON/FB_TOF/FB_TP/FB_TONR 四类定时器
- PT/ET 参数使用 DINT 类型（毫秒值），遵循 LSP-903
- 内部使用 IEC 定时器或自实现累加逻辑（视平台）
- 禁止使用 TIME 类型（LSP 编译器不支持）

#### 2.1.2 counter/（计数器模块）
- 实现 FB_CTU/FB_CTD/FB_CTUD 三类计数器
- 使用上升沿检测（edge/ 模块）触发计数
- 支持复位（R）与加载（LD）功能
- 计数值类型支持 INT 与 DINT

#### 2.1.3 edge/（边沿检测模块）
- 实现 FB_R_TRIG（上升沿）/FB_F_TRIG（下降沿）
- 内部静态变量保存上一周期 CLK 状态
- Q 输出持续一个扫描周期

#### 2.1.4 convert/（转换模块）
- 实现时间、数值类型间的转换
- 纯函数实现，无副作用
- 转换单位明确标注（毫秒/秒）
- 溢出处理：返回极值并记录日志

#### 2.1.5 actuator/（执行器模块）
- 封装气缸、电机、传送机构等执行元件
- 采用状态机设计（IDLE/RUNNING/DONE/ERROR）
- 集成超时检测（依赖 timer/ 模块）
- 输出错误码与故障标志

#### 2.1.6 communication/（通信模块）
- 实现设备握手协议（请求-应答机制）
- 依赖 edge/ 模块进行信号边沿检测
- 使用 types/ 中的 ST_HandshakeBits 结构体
- 支持超时与重试机制

#### 2.1.7 pulse/（脉冲生成模块）
- 实现 FB_TaktGenerator（节拍发生器）
- 可配置周期与占空比
- 依赖 timer/ 模块实现时序

#### 2.1.8 log/（日志模块）
- 实现 FC_LogMsg 日志输出函数
- 支持 INFO/WARN/ERROR 三级日志
- 格式化时间戳、级别、消息体

#### 2.1.9 types/（类型定义模块）
- 定义业务相关结构体（ST_Cylinder、ST_HandshakeBits 等）
- 字段命名小驼峰 + 类型前缀
- 每个结构体配套独立 .scl 文件

### 2.2 代码组织规范
- 每个 FB/FC 独立 .scl 文件
- 文件名与 FB/FC 名一致
- 复杂 FB 配套独立 PRD 子目录
- 注释遵循 LSP-904（英文半角标点、禁止嵌套）

## 3. 测试方案

### 3.1 单元测试
- **测试对象**：timer/、counter/、edge/、convert/ 模块
- **测试方法**：使用 PLC 仿真器（PLCSIM）或离线测试框架
- **测试用例**：
  - 定时器：边界值（0ms、最大值）、IN 信号抖动、复位行为
  - 计数器：边界值（0、PV）、递增递减混合、复位
  - 边沿检测：连续脉冲、信号抖动、初始状态
  - 转换函数：正常值、边界值、溢出

### 3.2 集成测试
- **测试对象**：actuator/、communication/ 模块
- **测试方法**：搭建测试台架，模拟上下游信号
- **测试场景**：
  - 气缸控制完整流程（伸出→到位→缩回→到位）
  - 设备握手完整流程（请求→应答→完成）
  - 故障注入与恢复

### 3.3 回归测试
- 每次版本发布前执行完整测试套件
- 修复 Bug 时必须添加对应回归测试用例
- 测试结果记录到 PRD 文档

## 4. 部署方案

### 4.1 库的引用方式
1. 将 {{ library_name }} 库目录放置到项目可访问的位置
2. 在项目的 .plc.json 中添加 libraries 路径：
   ```json
   {
     "libraries": ["../01_SharedLibraries/{{ library_name }}"]
   }
   ```
3. 路径使用相对路径，分隔符为正斜杠 `/`
4. 公共库本身的 .plc.json 中 libraries 为空数组（不引用其他库）

### 4.2 版本管理
- 库版本通过 .plc.json 的 version 字段管理
- 主版本变更需通知所有调用方
- 建议在项目文档中记录所依赖的库版本

### 4.3 分发方式
- 通过 Git 仓库管理，调用方 clone 或 submodule 引用
- 或通过文件共享方式分发
- 禁止直接修改库源码，应通过版本升级流程

## 5. 维护方案

### 5.1 版本管理流程
1. **变更申请**：提交变更需求到 PM_SESSION
2. **影响评估**：评估变更对调用方的影响
3. **实现与测试**：按规范实现并完成测试
4. **版本升级**：更新 .plc.json 版本号与 PRD 文档
5. **发布通知**：通知所有调用方升级

### 5.2 变更分类
| 变更类型 | 版本升级 | 兼容性 | 流程 |
|---------|---------|--------|------|
| Bug 修复 | 修订号 | 完全兼容 | 直接升级 |
| 新增功能块 | 次版本 | 向后兼容 | 通知调用方 |
| 接口变更 | 主版本 | 破坏兼容 | 评估+迁移 |

### 5.3 文档维护
- 每次变更必须同步更新 PRD 文档
- 接口变更必须更新 INT 文档版本号
- PM_SESSION 记录变更日志与实施日志

### 5.4 质量保障
- 提交前运行 LSP 规范检查（905/904/903/906）
- Git hooks 强制执行规范检查
- 定期审查库代码，清理废弃接口

## 6. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本 | auto-pm |
