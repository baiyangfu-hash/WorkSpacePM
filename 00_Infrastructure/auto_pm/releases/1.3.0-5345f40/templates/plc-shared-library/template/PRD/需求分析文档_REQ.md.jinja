# 需求分析文档（REQ）- {{ library_name }}

> 库名称: {{ library_name }}
> 库描述: {{ description }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}

## 1. 项目概述

### 1.1 库定位
{{ library_name }} 是一个 PLC 公共函数库，旨在为自动化项目提供可复用、标准化、跨项目共享的基础功能块（FB）、函数（FC）和类型定义（ST）。本库沉淀项目开发中高频使用、经过验证的通用逻辑，避免重复造轮子，提升 PLC 程序的开发效率与质量。

### 1.2 目标用户
- PLC 自动化开发工程师：在多个项目中复用通用功能块，减少重复编码
- 项目维护人员：依赖稳定的接口契约进行二次开发与维护
- 规范管理人员：通过统一的库版本管理，确保各项目使用一致的实现

### 1.3 核心价值
- **复用性**：一次开发，多项目复用，降低维护成本
- **标准化**：统一命名、接口、实现风格，遵循 LSP-905 编程规范
- **可靠性**：所有功能块经过单元测试与项目验证，保证逻辑正确性
- **可追溯**：每个 FB/FC 配套 PRD 文档，接口版本化管理

## 2. 功能需求

### 2.1 功能类别清单
{{ library_name }} 按功能类别组织，提供以下模块：

| 编号 | 模块目录 | 功能类别 | 说明 |
|------|---------|---------|------|
| F-001 | actuator/ | 执行器功能块 | 气缸控制、电机控制、传送机构等执行元件的封装 |
| F-002 | communication/ | 通信功能块 | 设备握手、信号交互、上下游通信协议封装 |
| F-003 | convert/ | 数据转换函数 | 时间、数值、类型之间的转换（如 TIME↔DINT） |
| F-004 | counter/ | 计数器功能块 | 递增、递减、双向计数器，支持复位与预设 |
| F-005 | edge/ | 边沿检测功能块 | 上升沿（R_TRIG）、下降沿（F_TRIG）检测 |
| F-006 | log/ | 日志函数 | 运行日志、报警日志的格式化输出 |
| F-007 | pulse/ | 脉冲生成功能块 | 周期脉冲、节拍发生器、可调占空比脉冲 |
| F-008 | timer/ | 定时器功能块 | 接通延时（TON）、断开延时（TOF）、脉冲（TP）、累加（TONR） |
| F-009 | types/ | 类型定义 | 传感器、执行器、握手等业务相关的结构体（ST）定义 |

### 2.2 功能详细描述
每个功能块/函数需满足以下要求：
- **单一职责**：一个 FB/FC 只负责一项明确功能，禁止混合业务逻辑
- **无状态优先**：FC 函数应为纯函数，相同输入产生相同输出
- **状态隔离**：FB 功能块的内部状态通过实例化隔离，禁止使用全局变量
- **参数完整**：所有 FB 调用必须包含完整参数（IN/PT/Q/ET 等），禁止省略

## 3. 非功能需求

### 3.1 兼容性需求
- 支持西门子 S7-1200/1500 系列 PLC（SCL 语言）
- 支持三菱 GX3 系列 PLC（ST 语言）
- 兼容 IEC 61131-3 标准，确保跨平台可移植性
- 遵循 LSP-907 项目配置规范，通过 .plc.json 管理库引用

### 3.2 可维护性需求
- 代码遵循 LSP-905 SCL 编程规范（命名、语法、结构）
- 注释遵循 LSP-904 注释规范（英文半角标点、禁止嵌套）
- 每个 FB/FC 配套独立的 PRD 文档（REQ/INT/DSN/TEC）
- 变量命名采用小驼峰风格，前缀与类型匹配（如 `i_bStart`、`q_rSpeed`）

### 3.3 可测试性需求
- 关键逻辑可独立测试，不依赖外部硬件
- 定时器、计数器等模块提供边界条件测试用例
- 类型定义（ST）字段语义清晰，便于构造测试数据

### 3.4 性能需求
- 单个 FB 扫描周期执行时间不超过 0.1ms（S7-1500）
- 库整体加载不显著增加项目编译时间
- 禁止在 FB 内部使用循环等待或阻塞调用

## 4. 接口需求

### 4.1 函数/功能块接口规范
- **FB 命名**：`FB_<编号>_<功能名>`（如 `FB_1011_CylinderControl`）或 `FB_<功能名>`（如 `FB_TON`）
- **FC 命名**：`FC_<功能名>`（如 `FC_DINT_TO_TIME`）
- **ST 命名**：`ST_<业务名>`（如 `ST_Cylinder`、`ST_HandshakeBits`）
- **参数命名**：输入前缀 `i_`、输出前缀 `q_`、InOut 前缀 `io_`、静态前缀 `s_`、临时前缀 `t_`

### 4.2 参数类型规范
- 定时器 PT/ET 参数使用 DINT 类型（毫秒值），禁止使用 TIME 类型（LSP-903）
- 布尔信号使用 BOOL 类型，数值使用 INT/DINT/REAL
- 复合数据使用 ST 结构体，通过 types/ 模块统一定义

## 5. 约束条件

### 5.1 平台约束
- 必须兼容西门子 TIA Portal SCL 编译器
- 禁止使用 METHOD 语法（Siemens LSP 插件不支持）
- 禁止使用 TIME 类型参数（LSP 编译器不支持）

### 5.2 规范约束
- 遵循 LSP-905 编程规范（命名、语法、代码结构）
- 遵循 LSP-904 注释规范（标点、嵌套禁令）
- 遵循 LSP-903 定时器使用规范
- 遵循 LSP-906 错误预防规则

### 5.3 范围约束
- 本库仅提供通用功能块，不包含业务特定逻辑
- 不包含硬件地址映射（由具体项目配置）
- 不包含 HMI 画面与通信协议实现

## 6. 验收标准

### 6.1 结构验收
- [ ] 目录结构完整（actuator/communication/convert/counter/edge/log/pulse/timer/types/PRD）
- [ ] .plc.json 配置正确（libraries 为空数组）
- [ ] 每个 FB/FC 配套 PRD 文档
- [ ] README.md 描述清晰

### 6.2 质量验收
- [ ] 所有代码通过 LSP-905 规范检查
- [ ] 所有注释通过 LSP-904 规范检查
- [ ] 定时器模块通过 LSP-903 规范检查
- [ ] 关键功能块具备单元测试用例

### 6.3 文档验收
- [ ] REQ 文档完整描述功能需求与非功能需求
- [ ] INT 文档完整描述接口规范与调用示例
- [ ] DSN 文档完整描述架构与详细设计
- [ ] TEC 文档完整描述技术方案与维护方案

## 7. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本 | auto-pm |
