# 需求分析文档（REQ）- {{ project_id }} {{ project_name }}

> 项目编号: {{ project_id }}
> 项目名称: {{ project_name }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}
> 项目类型: PLC 公共库验证项目（扁平结构）

## 1. 项目概述

### 1.1 验证目的
本项目 {{ project_id }} {{ project_name }} 旨在对公共库（SysLib）进行系统性验证，确保公共库中各功能块（FB）、函数（FC）和数据结构在目标 PLC 平台上行为符合设计预期。{{ description }}。通过搭建独立的测试项目，以可重复、可追溯的方式执行验证用例，为公共库的版本发布提供质量依据，防止缺陷流入生产项目。

### 1.2 验证范围
验证范围覆盖 SysLib 公共库中所有已注册的功能块与函数，包括但不限于：
- 定时器类功能块（TON/TOF/TONR 等）的时序逻辑与累加精度
- 边沿检测类功能块（R_TRIG/F_TRIG）的触发可靠性
- 数据处理类函数（限幅、标量变换、滤波）的数值正确性
- 状态机类功能块的状态迁移与异常恢复
- 通信辅助类功能块的接口契约

不在本次验证范围内的内容：生产环境业务逻辑、HMI 画面交互、现场 IO 物理接线验证。

### 1.3 验证方法
采用分层验证策略：
1. **单元验证**：针对单个 FB/FC 设计独立测试用例，隔离外部依赖，验证边界条件与典型路径。
2. **集成验证**：将多个 FB 组合调用，验证功能块间的协作与数据流传递。
3. **回归验证**：公共库版本升级后，全量重跑历史用例，确保无回归缺陷。
4. **对比验证**：将测试输出与预期值（黄金样本）逐周期比对，记录偏差。

## 2. 功能需求

### 2.1 功能清单
| 编号 | 功能名称 | 优先级 | 描述 |
|------|---------|--------|------|
| F-001 | 定时器功能块验证 | P0 | 验证 TON/TOF/TONR 在 PT 设定、复位、掉电保持场景下的输出时序 |
| F-002 | 边沿检测功能块验证 | P0 | 验证 R_TRIG/F_TRIG 在单周期脉冲、连续脉冲、初始状态下的触发行为 |
| F-003 | 数据处理函数验证 | P1 | 验证限幅、标量变换、一阶滤波函数的数值精度与边界处理 |
| F-004 | 状态机功能块验证 | P1 | 验证状态迁移合法性、非法状态恢复、超时转移逻辑 |
| F-005 | 测试调度框架 | P0 | 在 OB1 中实现测试用例的顺序调度、结果采集与失败暂停 |
| F-006 | 测试结果记录 | P1 | 将每条用例的通过/失败状态与关键中间量写入 DB1 供上位机读取 |

### 2.2 功能详细描述
**F-001 定时器功能块验证**：为每个待测定时器 FB 实例化一个背景 DB，通过 OB1 在指定周期置位 IN 输入，并在预设周期数后校验 Q 输出与 ET 累计时间。需覆盖 PT=0、PT=最大值、IN 在计时过程中反复跳变等边界场景。

**F-002 边沿检测功能块验证**：构造输入信号序列，包含上升沿、下降沿、单周期窄脉冲与电平保持，验证 CLK 输入变化时 Q 输出仅在对应沿的下一周期为 TRUE。需特别验证首次扫描（FirstScan）时的初始状态。

**F-005 测试调度框架**：OB1 中维护一个测试步骤计数器，每个扫描周期执行当前步骤的激励与判定，通过后自动推进到下一步骤；失败时置位全局失败标志并停止推进，便于定位失败点。

## 3. 非功能需求

### 3.1 测试覆盖率
- 功能块覆盖率：被测 FB 数量 / SysLib 注册 FB 总数 ≥ 90%
- 用例覆盖率：每个被测 FB 至少包含 3 条用例（正常路径、边界、异常）
- 分支覆盖率：关键 FB 的内部条件分支均需被至少一条用例触发

### 3.2 性能基准
- 单条用例执行周期数 ≤ 100 个扫描周期（默认 10ms 周期下 ≤ 1s）
- 全量用例集执行总时长 ≤ 5 分钟
- OB1 循环扫描周期增量 ≤ 2ms（测试逻辑不得导致周期超时）

### 3.3 可靠性需求
- 测试项目本身不得引入死循环、数组越界、指针异常等导致 PLC 停机的缺陷
- 测试用例间相互隔离，前序用例失败不得影响后续用例的独立执行
- 掉电重启后测试框架能从首条用例重新开始，不残留中间状态

## 4. 接口需求

### 4.1 内部接口
- 测试项目通过 `libraries` 配置引用 `../01_SharedLibraries/SysLib`，调用其中的 FB/FC
- OB1 与 DB1 之间通过符号访问交换测试步骤号、结果标志与计数器

### 4.2 外部接口
- 数字量输入：测试启动按钮（%I0.0）、测试复位按钮（%I0.1）
- 数字量输出：全部通过指示灯（%Q0.0）、失败指示灯（%Q0.1）、运行指示灯（%Q0.2）
- 通信接口：可选，通过以太网将 DB1 中的测试结果上传至上位机（协议待定）

## 5. 约束条件

### 5.1 测试环境
- PLC 型号：西门子 S7-1200/1500 系列（固件版本 V4.0 及以上）
- 编程软件：TIA Portal V16 及以上
- 编程语言：SCL/ST（遵循 IEC 61131-3 与 LSP-905 规范）
- 项目结构：扁平结构（DB1/OB1/Test），遵循 LSP-907 规范

### 5.2 依赖约束
- SysLib 公共库版本必须与测试项目声明的版本一致
- 测试项目不得修改 SysLib 源码，仅通过实例化与调用进行验证

## 6. 验收标准

### 6.1 通过准则
- [ ] 全部 P0 用例（F-001、F-002、F-005）执行通过，无失败
- [ ] P1 用例通过率 ≥ 95%，失败项均有明确原因分析与处置结论
- [ ] 测试覆盖率指标（3.1 节）全部达标
- [ ] OB1 扫描周期增量满足 3.2 节基准
- [ ] 测试结果可被上位机读取并生成验证报告

### 6.2 交付物
- 测试项目源码（DB1/OB1/Test 目录）
- 验证报告（含每条用例的预期值、实际值、判定结果）
- 缺陷清单（如有，含复现步骤与严重等级）

## 7. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本，由 plc-test-suite 模板生成 | auto-pm |
