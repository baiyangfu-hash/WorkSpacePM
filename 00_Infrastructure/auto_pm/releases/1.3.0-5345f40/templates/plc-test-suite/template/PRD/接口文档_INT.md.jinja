# 接口文档（INT）- {{ project_id }} {{ project_name }}

> 项目编号: {{ project_id }}
> 项目名称: {{ project_name }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}
> 项目类型: PLC 公共库验证项目（扁平结构）

## 1. 接口概述

### 1.1 接口设计原则
本项目 {{ project_id }} 作为公共库 SysLib 的验证项目，接口设计遵循"最小外部依赖、最大内部可观测"原则。测试项目通过 `libraries` 配置引用 `../01_SharedLibraries/SysLib`，以符号方式调用其中的功能块与函数，不直接访问 SysLib 的内部变量。所有测试激励由 OB1 内部逻辑产生，所有测试结果通过 DB1 暴露，便于上位机读取与判定。

### 1.2 接口清单
| 编号 | 接口名称 | 类型 | 方向 | 描述 |
|------|---------|------|------|------|
| I-001 | 测试启动 | 数字量输入 | 外部→OB1 | 上升沿触发测试用例集从头执行 |
| I-002 | 测试复位 | 数字量输入 | 外部→OB1 | 上升沿清零所有测试状态与结果 |
| I-003 | 全部通过指示 | 数字量输出 | OB1→外部 | 全部用例通过时为 TRUE |
| I-004 | 失败指示 | 数字量输出 | OB1→外部 | 任一用例失败时为 TRUE 并保持 |
| I-005 | 运行指示 | 数字量输出 | OB1→外部 | 测试进行中闪烁，空闲时常亮 |
| I-006 | 测试结果数据块 | 数据块 | OB1→上位机 | DB1 存放步骤号、通过数、失败数、失败步骤号 |
| I-007 | SysLib 调用 | 库引用 | OB1→SysLib | 通过 libraries 配置引用公共库 |

## 2. 数字量输入（DI）

| 信号名称 | 地址 | 上游设备 | 信号说明 | 备注 |
|---------|------|---------|---------|------|
| diTestStart | %I0.0 | 测试面板按钮 | 上升沿启动测试，仅在空闲态有效 | 去抖 20ms |
| diTestReset | %I0.1 | 测试面板按钮 | 上升沿复位测试状态，任意时刻有效 | 去抖 20ms |
| diModeAuto | %I0.2 | 拨码开关 | TRUE=自动连续执行，FALSE=单步执行 | 单步模式下每按一次启动执行一条 |

## 3. 数字量输出（DO）

| 信号名称 | 地址 | 下游设备 | 信号说明 | 备注 |
|---------|------|---------|---------|------|
| doAllPassed | %Q0.0 | 绿色指示灯 | 全部用例通过时点亮 | 复位后熄灭 |
| doFailed | %Q0.1 | 红色指示灯 | 任一用例失败时点亮并保持 | 需手动复位清除 |
| doRunning | %Q0.2 | 黄色指示灯 | 测试进行中 0.5Hz 闪烁，空闲时常亮 | 表征 PLC 运行状态 |
| doStepPulse | %Q0.3 | 示波器/记录仪 | 每完成一条用例输出一个周期窄脉冲 | 用于外部计数校验 |

## 4. 模拟量输入（AI）

本项目为纯逻辑验证，默认不使用模拟量输入。如被测函数涉及模拟量（如标量变换），通过 OB1 内部赋值模拟，不占用物理 AI 通道。

| 信号名称 | 地址 | 量程 | 单位 | 上游设备 | 备注 |
|---------|------|------|------|---------|------|
| （预留） | %IW0 | 0-27648 | 原始值 | 内部赋值 | 供被测函数验证使用 |

## 5. 模拟量输出（AO）

| 信号名称 | 地址 | 量程 | 单位 | 下游设备 | 备注 |
|---------|------|------|------|---------|------|
| （预留） | %QW0 | 0-27648 | 原始值 | 万用表 | 输出被测函数处理结果供外部核对 |

## 6. 通信接口

### 6.1 以太网通信（结果上传）
- 协议: S7 通信 / TCP
- 本机IP: 192.168.0.10（默认，可改）
- 上位机IP: 192.168.0.100（默认，可改）
- 端口: 102
- 用途: 上位机读取 DB1 中的测试结果数据块，生成验证报告
- 数据格式: DB1 结构化符号访问，详见 DSN 文档第 4 节

### 6.2 库引用接口
- 引用方式: `.plc.json` 中 `libraries` 字段声明 `../01_SharedLibraries/SysLib`
- 路径含义: 测试项目位于工作空间根级，回退 1 级到工作空间根，再进入 `01_SharedLibraries/SysLib`
- 版本约束: SysLib 版本须与测试项目声明的版本一致，不一致时 OB1 首扫描报错

## 7. 测试用例接口规范

### 7.1 用例调用约定
每个测试用例在 OB1 中以一个代码区域实现，遵循统一结构：
1. **激励阶段**：构造被测 FB/FC 的输入参数
2. **调用阶段**：调用被测 FB/FC（含背景 DB 实例化）
3. **判定阶段**：将实际输出与预期值比较，结果写入 DB1 对应字段
4. **推进阶段**：通过则步骤号 +1，失败则置位失败标志并停止推进

### 7.2 用例编号规则
- 格式：`TC-<FB编号>-<序号>`，如 `TC-001-01` 表示 FB-001 的第 1 条用例
- 每条用例对应 DB1 中一条记录，含用例编号、预期值、实际值、判定结果

## 8. 版本兼容性说明

- 测试项目与 SysLib 通过语义化版本绑定，主版本号不一致时不允许执行
- SysLib 新增 FB 时，测试项目需补充对应用例并升级版本号
- SysLib 废弃 FB 时，测试项目需标记对应用例为"已废弃"并保留历史记录
- 接口变更（FB 形参增减）视为破坏性变更，需同步更新测试用例与本文档

## 9. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本，由 plc-test-suite 模板生成 | auto-pm |
