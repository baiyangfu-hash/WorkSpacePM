# 技术方案文档（TEC）- {{ project_id }} {{ project_name }}

> 项目编号: {{ project_id }}
> 项目名称: {{ project_name }}
> 版本: {{ version }}
> 创建日期: {{ '%Y-%m-%d %H:%M:%S' | strftime }}
> 项目类型: PLC 公共库验证项目（扁平结构）

## 1. 技术选型

### 1.1 PLC 平台选型
本项目 {{ project_id }} {{ project_name }} 选用西门子 S7-1200/1500 系列 PLC 作为验证平台，原因如下：
- S7-1200/1500 是工作空间内 PLC 项目的主力平台，SysLib 公共库即面向该平台开发
- SCL/ST 编程语言原生支持，符合 IEC 61131-3 标准与 LSP-905 规范
- TIA Portal 提供完善的在线监控与变量跟踪能力，便于调试测试用例
- 固件 V4.0+ 支持结构化 DB 访问与数组操作，满足测试结果记录需求

### 1.2 测试框架选型
采用"OB1 内嵌调度框架"方案，而非独立测试工具（如 PLCSIM Advanced 自动化测试），原因如下：
- 内嵌框架不依赖额外软件许可，任何 TIA Portal 环境均可运行
- 测试逻辑与被测库在同一项目内，符号访问直接，无需配置通信
- 框架轻量（仅 CASE 分支 + 计数器），对 OB1 扫描周期影响可控
- 缺点是不支持全自动批量回归，需配合上位机脚本实现结果采集（见第 5 节）

### 1.3 编程语言与规范
- 编程语言：SCL（结构化控制语言），用于 OB1 与测试逻辑
- 编码规范：遵循 LSP-905 SCL 编程规范（命名、注释、代码结构）
- 项目结构：遵循 LSP-907 扁平结构变体（DB1/OB1/Test 平级）
- 命名规范：匈牙利命名法 + 业务语义，如 `uiTestStep`（UINT 步骤号）、`bAllPassed`（BOOL 标志）

## 2. 实现方案

### 2.1 测试用例编写规范
每条测试用例在 OB1 的 CASE 分支中实现，遵循"四段式"结构：

**段 1 - 激励**：构造被测 FB/FC 的输入条件。例如对 TON 的测试，置位 IN 输入并设定 PT 值。
```
// 用例 TC-001-01：TON 基本定时
"FB_TON_Instance".IN := TRUE;
"FB_TON_Instance".PT := T#2s;
```

**段 2 - 调用**：调用被测 FB（含背景 DB 实例化）。
```
"FB_TON_Instance"();
```

**段 3 - 判定**：在预期周期数到达后，采样实际输出并与预期比较。
```
IF "DB1".dwCycleCounter >= 200 THEN  // 2s / 10ms = 200 周期
    IF "FB_TON_Instance".Q = TRUE THEN
        // 通过
    ELSE
        // 失败
    END_IF;
END_IF;
```

**段 4 - 推进**：写结果到 DB1，通过则步骤号+1，失败则跳转失败态。

### 2.2 用例编号与步骤映射
用例编号 `TC-<FB编号>-<序号>` 与 OB1 步骤号一一映射，映射表在 OB1 声明区以注释维护：
```
// 步骤映射表：
//   1 = TC-001-01 (TON 基本定时)
//   2 = TC-001-02 (TON 复位)
//   3 = TC-002-01 (R_TRIG 上升沿)
//   ...
```

### 2.3 边界条件处理
- **首次扫描**：FirstScan 时所有被测 FB 处于初始状态，用例需显式处理初始 Q/ET 值
- **掉电保持**：TONR 类用例需验证掉电后 ET 累计值是否保持，通过模拟掉电（Stop→Run）测试
- **最大值边界**：PT 设定为 TIME 最大值（T#24d20h31m23s647ms）时验证不溢出
- **零值边界**：PT=0 时验证 Q 立即跟随 IN

## 3. 测试方案

### 3.1 单元测试
针对单个 FB/FC 设计独立用例，隔离外部依赖。每个 FB 至少 3 条用例：
- 正常路径：典型输入下的标准行为
- 边界条件：最小/最大/零值输入
- 异常恢复：非法状态注入后的恢复能力

### 3.2 集成测试
将多个 FB 组合调用，验证协作行为。例如：
- TON 输出驱动 R_TRIG，验证定时到达时的单周期脉冲
- 限幅函数输出送入标量变换，验证链式处理数值正确性
- 状态机 FB 的状态输出控制 TON 的使能，验证状态迁移与时序配合

### 3.3 回归测试
SysLib 版本升级后，全量重跑所有历史用例。回归测试流程：
1. 更新 `.plc.json` 中 libraries 指向的 SysLib 版本
2. 下载项目到 PLC，执行 diTestReset 清零状态
3. 触发 diTestStart 全量执行
4. 上位机读取 DB1，与上次结果对比，标记差异项
5. 差异项需分析是否为预期变更（接口修改）或回归缺陷

### 3.4 对比测试
对数值类函数，将 PLC 实际输出与离线计算的黄金样本逐点比对。黄金样本以 CSV 形式存储，覆盖输入域的等价类划分点。

## 4. 部署方案

### 4.1 测试项目部署到 PLC
1. 在 TIA Portal 中打开由本模板生成的项目
2. 确认 `.plc.json` 中 `libraries` 路径 `../01_SharedLibraries/SysLib` 可正确解析
3. 编译项目，确认无错误（库符号全部解析成功）
4. 下载到 PLC（硬件配置需与实际 CPU 匹配）
5. 在线监控 DB1，确认 `uiTestStep=0`（空闲态）

### 4.2 上位机部署
1. 上位机配置 S7 通信，连接 PLC 的 IP:102
2. 配置 DB1 的符号地址映射
3. 运行结果采集脚本，周期性读取 DB1
4. 测试完成后生成验证报告（HTML/PDF）

### 4.3 环境检查清单
- [ ] PLC CPU 型号与固件版本符合 5.1 节约束
- [ ] SysLib 版本与测试项目声明一致
- [ ] 网络连通性（ping PLC IP 成功）
- [ ] 物理按钮接线正确（diTestStart/diTestReset）
- [ ] 指示灯接线正确（doAllPassed/doFailed/doRunning）

## 5. 维护方案

### 5.1 测试用例更新
- SysLib 新增 FB 时：在 OB1 的 CASE 分支追加对应用例，更新步骤映射表与 `uiTotalCases`
- SysLib 修改 FB 形参时：同步修改用例中的调用代码与预期值，升级测试项目版本号
- 用例废弃时：不删除代码，标记为"已废弃"并跳过执行（步骤号直接 +1）

### 5.2 版本管理
- 测试项目与 SysLib 通过版本号绑定，版本号遵循语义化版本（SemVer）
- 每次用例变更需提交 git commit，遵循 `git-commit-message.md` 规范
- 回归测试报告归档至 `09_整改项/archive/iterations/` 目录

### 5.3 缺陷处理流程
1. 用例失败时，先在 OB1 在线监控中复现并定位失败点
2. 判定缺陷归属：SysLib 缺陷 / 测试用例缺陷 / 环境问题
3. SysLib 缺陷：提交 issue 至 SysLib 项目，修复后回归
4. 测试用例缺陷：修正用例预期值或激励逻辑，重新执行
5. 环境问题：排查接线/网络/固件，恢复后重新执行

## 6. 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|---------|--------|
| {{ '%Y-%m-%d %H:%M:%S' | strftime }} | {{ version }} | 初始版本，由 plc-test-suite 模板生成 | auto-pm |
