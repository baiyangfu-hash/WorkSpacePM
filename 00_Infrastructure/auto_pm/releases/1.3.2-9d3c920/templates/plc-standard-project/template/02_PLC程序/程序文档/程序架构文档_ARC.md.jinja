# 程序架构文档 - {{ project_id }} {{ project_name }}

> **项目编号**: {{ project_id }}
> **项目名称**: {{ project_name }}
> **文档版本**: {{ version }}
> **编制日期**: {{ '%Y-%m-%d' | strftime }}
> **遵循规范**: IEC 61131-3, LSP-905 SCL编程规范, LSP-907 项目结构规范

## 1. 文档基础信息

| 属性 | 值 |
|------|-----|
| **文档标题** | {{ project_name }} 程序架构文档 |
| **文档版本** | {{ version }} |
| **编制日期** | {{ '%Y-%m-%d' | strftime }} |
| **适用范围** | {{ project_name }} 项目 ({{ project_id }}) |
| **架构版本** | {{ version }} (扁平化组件架构) |

## 2. 架构概述

{{ description }}

本项目采用**扁平化组件架构**，主控程序 OB1 作为唯一协调层，直接调用各工站功能块。所有 I/O 映射通过 GlobalVars 数据块集中管理，工站功能块为纯逻辑块，不直接依赖物理地址。

### 2.1 架构特点

- **扁平结构**: 1 个 OB + 多个 FB，无多层嵌套
- **I/O 集中映射**: 所有物理地址在 GlobalVars 中统一管理
- **工站解耦**: 各工站 FB 为纯逻辑块，接口清晰
- **易于维护**: 新增工站只需新增一个 FB
- **符合开闭原则**: 扩展开放，修改封闭

## 3. 架构对比

```
+--------------------------- 扁平化组件架构 ({{ version }}) ---------------------------+
|                                                                             |
|  主控程序 (OB1) -- 唯一协调层                                                |
|  │                                                                           |
│  ├── 职责1: 初始化 (FirstScan)                                               │
│  ├── 职责2: 工站实例化与调用 (按工艺顺序)                                     │
│  ├── 职责3: 安全联锁逻辑                                                     │
│  │                                                                           │
│  ├──→ fbConveyor    (输送机控制)     ← 纯逻辑块，无物理地址依赖              │
│  ├──→ fbPickPlace   (取放料控制)     ← 纯逻辑块，无物理地址依赖              │
│  ├──→ fbFeeder      (送料控制)       ← 纯逻辑块，无物理地址依赖              │
│  ├──→ fbExternal    (外部设备交互)   ← 纯逻辑块，无物理地址依赖              │
│  └──→ fbCommonAlarm (公共报警)       ← 纯逻辑块，无物理地址依赖              │
|                                                                             |
|  总计: 1个OB + 5个FB + 1个DB = 7个组件                                       |
|  特点: 扁平结构、IO集中映射、工站解耦、易于维护                                 |
+-----------------------------------------------------------------------------+
```

## 4. 组件清单

| 序号 | 组件 | 类型 | 文件路径 | 核心职责 |
|------|------|------|----------|----------|
| 1 | **OB1** | OB | `02_PLC程序/PLC_ST/OB1/OB1.scl` | 主循环、FB 调用顺序管理、安全联锁 |
| 2 | **GlobalVars** | DB | `02_PLC程序/PLC_ST/DB1/GlobalVars.db` | 全局变量、I/O 映射中心 |
| 3 | **fbConveyor** | FB | `02_PLC程序/PLC_ST/conveyor/` | 输送机控制 (分料/输送) |
| 4 | **fbPickPlace** | FB | `02_PLC程序/PLC_ST/pickplace/` | 取放料控制 (Z轴+X1轴+夹爪) |
| 5 | **fbFeeder** | FB | `02_PLC程序/PLC_ST/feeder/` | 送料控制 (X2轴+下游交互) |
| 6 | **fbExternal** | FB | `02_PLC程序/PLC_ST/external/` | 外部设备交互 (上游/下游) |
| 7 | **fbCommonAlarm** | FB | `02_PLC程序/PLC_ST/common/` | 公共报警管理 (汇总+MES) |
| 8 | **工程资产样例** | Asset | `02_PLC程序/工程资产/` | IO 点表、程序块、通讯资产占位 |

## 5. 数据流架构

### 5.1 I/O 映射流程

```
  物理输入 (X地址) → GlobalVars 结构体 → 工站 FB 输入参数
                                              ↓
                                         工站 FB 处理
                                              ↓
  物理输出 (Y地址) ← GlobalVars 结构体 ← 工站 FB 输出参数
```

### 5.2 工站间信号传递

```
  conveyor.q_bLayerFeedDone → pickplace.i_bLayerFeedDone
  pickplace.q_bPlaceDoneToFeeder → feeder.i_bPlaceDone
  各工站.q_iAlarmCode → common.i_iXXXAlarm
```

## 6. 目录结构

```
02_PLC程序/
├── PLC_ST/                        # PLC 项目根 (.plc.json 所在)
│   ├── .plc.json                  # PLC 项目配置
│   ├── DB1/
│   │   └── GlobalVars.db          # 全局变量数据块
│   ├── OB1/
│   │   └── OB1.scl                # 主循环程序
│   ├── Test/                      # 测试用例
│   ├── common/                    # 公共功能块
│   ├── conveyor/                  # 输送机功能块
│   ├── external/                  # 外部设备交互功能块
│   ├── feeder/                    # 送料功能块
│   └── pickplace/                 # 取放料功能块
├── 工程资产/
│   ├── io_points.csv              # IO 点表
│   ├── program_blocks.yml         # 程序块清单
│   └── communications.yml         # 通讯对象清单
└── 程序文档/                      # 程序设计文档
    ├── 016_PLC程序设计总文档_PLC.md
    ├── 018_自动工艺流程图_FLOW.md
    ├── 程序架构文档_ARC.md
    ├── PLC变量定义文档_VAR.md
    ├── 015_IO分配表_IO.md
    └── 详细设计说明书_DSN.md
```

## 7. 关键技术特点

### 7.1 纯逻辑块设计

所有工站 FB 为纯逻辑块，不直接引用物理地址 (X/Y/M/D)，所有 I/O 通过 GlobalVars 结构体传递。优点：
- 可移植性强（更换 PLC 平台只需修改 GlobalVars 映射）
- 可测试性强（可通过 GlobalVars 注入测试数据）
- 维护性好（逻辑与硬件解耦）

### 7.2 集中 I/O 映射

所有物理地址映射集中在 GlobalVars 数据块中，便于：
- 统一管理 I/O 地址
- 快速定位硬件变更影响范围
- 生成 I/O 分配表

### 7.3 状态机内聚

每个工站 FB 内置完整的状态机，工站间通过信号传递协调，符合高内聚低耦合原则。

## 8. 版本更新记录

| 版本 | 日期 | 更新内容 | 编制人 |
|------|------|----------|--------|
| {{ version }} | {{ '%Y-%m-%d' | strftime }} | 初始版本 | auto-pm |
